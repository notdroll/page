<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>{% if title %}{{ title }} — droll{% else %}droll — home{% endif %}</title>

  <meta name="description" content="{{ description | default: 'Minimal personal site for essays, work, and favorites.' }}"/>
  <meta property="og:title" content="{{ title | default: 'droll' }}"/>
  <meta property="og:description" content="{{ description | default: 'Minimal personal site for essays, work, and favorites.' }}"/>
  <meta property="og:image" content="{{ image | default: '/assets/og.jpg' | url }}">
  <meta name="twitter:card" content="summary_large_image">

  <link rel="stylesheet" href="/assets/css/main.css?v=2">
  {% if canonical %}<link rel="canonical" href="{{ canonical }}"/>{% endif %}
</head>
<body>
  <div class="shell">

    <aside class="sidebar">
      <div class="sidebar-inner">
        <h1 class="site-name"><a href="{{ '/' | url }}">droll</a></h1>
        <p class="site-sub">a token lawyer, but not a token lawyer</p>

        <nav class="side-nav">
          <a href="{{ '/about/' | url }}">About</a>
          <a href="{{ '/papers/' | url }}">Papers</a>
          <a href="{{ '/work/' | url }}">Work</a>
          <a href="{{ '/favorites/' | url }}">Favorites</a>
          <a href="{{ '/contact/' | url }}">Contact</a>
        </nav>

        <div class="side-links">
          <a href="https://x.com/notdroll" target="_blank" rel="me noopener">X</a>
          <span class="dot">·</span>
          <a href="{{ '/sitemap.xml' | url }}">Sitemap</a>
        </div>
      </div>
    </aside>

    <main id="content" class="content">
      {{ content }}
      <footer class="site-footer">© {{ "now" | date: "%Y" }} droll</footer>
    </main>

  </div>
</body>
</html>
